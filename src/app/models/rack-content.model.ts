import {BaseType} from './common/base-type.model';

export class RackContent extends BaseType {
  identifier?: string;
  identifierPartI?: string;
  identifierPartU?: string;
  identifierPartA?: string;
  regex?: boolean;
}
